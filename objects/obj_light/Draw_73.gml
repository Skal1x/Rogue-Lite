/*
if (surface_exists(surf)) {
	var cameraX = round(camera_get_view_width(view_camera[0]) - 2);
	var cameraY = round(camera_get_view_height(view_camera[0]) - 2);
} else {
	surf = surface_create(camera_get_view_width(view_camera[0]) + 4, camera_get_view_height(view_camera[0]) + 4);
	
}
*/